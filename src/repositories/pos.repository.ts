/**
 * POS repository.
 * TODO: Implement queries for pos_sales, pos_sale_items.
 */

export async function getPosSalesByBranch(brandId: number, branchId: string) {
  console.log(`[PosRepository] getPosSalesByBranch — not implemented`);
  return [];
}

export async function getPosSaleById(id: string) {
  console.log(`[PosRepository] getPosSaleById — not implemented`);
  return null;
}
