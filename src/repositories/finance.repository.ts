/**
 * Finance repository.
 * TODO: Implement queries for finance_ledger and reporting views.
 */

export async function getDailyFinanceSummary(brandId: number, branchId?: string) {
  console.log(`[FinanceRepository] getDailyFinanceSummary — not implemented`);
  return [];
}

export async function getMonthlyFinanceSummary(brandId: number, branchId?: string) {
  console.log(`[FinanceRepository] getMonthlyFinanceSummary — not implemented`);
  return [];
}

export async function getBranchRevenueSummary(brandId: number) {
  console.log(`[FinanceRepository] getBranchRevenueSummary — not implemented`);
  return [];
}
