/**
 * Inventory repository.
 * TODO: Implement queries for inventory_items, branch_inventory_stocks, inventory_categories.
 */

export async function getItemsByBrandId(brandId: number) {
  console.log(`[InventoryRepository] getItemsByBrandId — not implemented`);
  return [];
}

export async function getBranchStocks(brandId: number, branchId: string) {
  console.log(`[InventoryRepository] getBranchStocks — not implemented`);
  return [];
}

export async function getCategoriesByBrandId(brandId: number) {
  console.log(`[InventoryRepository] getCategoriesByBrandId — not implemented`);
  return [];
}
